export enum Order {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum LikeEntityType {
  DISCUSSION = 'DISCUSSION',
  COMMENT = 'COMMENT',
}

export enum LikeType {
  LIKE = 'LIKE',
  DISLIKE = 'DISLIKE',
}
